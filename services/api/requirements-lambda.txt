# Runtime dependencies bundled into the Lambda dependency layer.
# Intentionally excludes uvicorn[standard] (only used by local dev) and boto3
# (already provided by the Lambda runtime) to keep the layer small.
fastapi>=0.111
pydantic>=2.7
pydantic-settings>=2.3
mangum>=0.17
